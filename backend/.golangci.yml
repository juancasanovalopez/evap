version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - errcheck
    - ineffassign
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-dirs:
    - test/integration
