version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - errcheck
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    paths:
      - test/integration

formatters:
  enable:
    - gofmt
    - goimports
