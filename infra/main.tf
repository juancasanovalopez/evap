# Entry point placeholder: resources are organized by concern across
# dynamodb.tf, ssm.tf, iam.tf, lambda.tf, apigateway.tf, and oidc.tf.
# See provider.tf for the AWS provider configuration and versions.tf for
# required provider/Terraform versions.
